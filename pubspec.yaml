name: get_stacked
description: An implementation of the Stacked package using Get. It gives you all the amazing utility classes from the Stacked package but without the dependency on provider.
version: 1.0.8
homepage: https://github.com/Prn-Ice/get_stacked

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  get: ^3.12.1

  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

# The following section is specific to Flutter.
flutter: